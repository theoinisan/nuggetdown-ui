// @flow
import * as React from "react";

import { StyledWrapper, StyledInner } from "./Styled";

const Topbar = () => (
  <StyledWrapper>
    <StyledInner>Hello</StyledInner>
  </StyledWrapper>
);

export default Topbar;
